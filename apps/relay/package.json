{
  "name": "@summonerkit/relay",
  "version": "0.11.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@summonerkit/remote": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260822.1",
    "wrangler": "4.125.0"
  }
}
